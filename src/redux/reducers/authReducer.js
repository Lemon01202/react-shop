import React from 'react'

const initialState = {

}

const authReducer = (state = initialState,action) => {
	switch (action.type) {
		case value:
			
			break;
	
		default:
			return state
	}
}

export default authReducer
